import { InjectionToken } from '@angular/core';
import { Router } from '@angular/router';
export declare type UseHash = {
    useHash: boolean;
};
export declare const SH_BROWSER_STORAGE: InjectionToken<Storage>;
export declare const USE_HASH: InjectionToken<UseHash>;
export declare class ShRedirectService {
    private router;
    private storage;
    private isHash;
    constructor(router: Router, storage: Storage, isHash: UseHash);
    readonly redirectPath: string;
    readonly queryParams: URLSearchParams | null;
    setRedirectPath(): void;
    redirect(): void;
}
