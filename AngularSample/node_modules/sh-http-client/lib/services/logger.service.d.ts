import { ShHttpClientService } from './sh-http-client.service';
import { Router } from '@angular/router';
declare type config = {
    url: string;
    useHash: boolean;
};
export declare class LoggerService {
    private http;
    private router;
    private isBackButton;
    private config;
    constructor(http: ShHttpClientService, router: Router);
    /**
     * logApiへリクエスト
     *
     */
    private push;
    /**
     * 設定を更新
     */
    configure(config: Partial<config>): this;
    /**
     * 画面遷移が発生するたびにlogging
     */
    trackingPath(): void;
    /**
     * actionlog pathは必須のため必ず送られる
     */
    action(action: string): Promise<void>;
    /**
     *
     */
    stopLoggingPath(): void;
}
export {};
