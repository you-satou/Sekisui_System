import { Alert } from '../utilities/Alert';
export declare class AlertHandlerService {
    private Alert;
    private AlertHandlerSubject;
    AlertHandlerState: import("rxjs").Observable<Alert>;
    /**
     * コンストラクタ
     */
    constructor();
    /**
    * アラートを追加する
    * @param validationType 検証結果タイプ
    * @param validationMessages 検証結果メッセージ配列
    */
    setAlert(alertInfo: Alert): void;
    /**
     * アラートをクリアする
     */
    alertClear(): void;
}
