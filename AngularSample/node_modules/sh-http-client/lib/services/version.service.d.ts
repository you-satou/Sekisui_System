import { HttpClient } from '@angular/common/http';
export declare class VersionService {
    private http;
    private versions;
    constructor(http: HttpClient);
    /**
     * versionの初期化
     */
    initVersion(...urls: string[]): Promise<void>;
    /**
     * urlからキーを生成
     * @param url
     */
    private getKeyUrl;
    /**
     * バックエンドのversion情報を取得
     */
    fetchVersion(url: string): Promise<string>;
    /**
     * urlをsplitした値を返す
     */
    private splitByApiUrl;
    /**
     * キャッシュしているversionをURLから取得する
     * @param url
     */
    getVersion(url: string): Promise<string>;
    /**
     * 受け取ったversionNoをキャッシュする
     * @param url apiUrl
     * @param versionNo バックエンドのversion
     */
    setVersionNo(url: string, versionNo: string): void;
}
