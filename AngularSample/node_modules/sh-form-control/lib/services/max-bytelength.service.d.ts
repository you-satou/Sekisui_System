export declare class MaxByteLengthService {
    private static readonly allowKeys;
    private static readonly allowCodes;
    private static readonly allowWithCtlKey;
    private static readonly allowWithShiftKey;
    constructor();
    generateByteInfo(value: string): {
        length: number;
        bytes: any[];
    };
    calcMaxLength(bytes: number[], maxBytelength: number, total?: number): number;
    static isAllowOperation(e: KeyboardEvent): boolean;
    static isAllowKey(key: string): boolean;
    private static isAllowCodes;
    private static isAllowWithCtlKey;
    private static isAllowWithShiftKey;
}
