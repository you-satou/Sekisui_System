export declare class LockScreenService {
    private isConnecting;
    private isConnectingSubject;
    isConnectingState: import("rxjs").Observable<boolean>;
    count: number;
    /**
    * コンストラクタ
    */
    constructor();
    /**
    * 画面をロックする(ユーザーが操作出来ないようにする)
    */
    lockScreen(): void;
    /**
    * 画面のロックを解除する(ユーザーが操作出来る状態にする)
    */
    unlock(): void;
}
