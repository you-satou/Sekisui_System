import { ShCtlApiService } from './sh-ctl-api.service';
/**
 * 区分マスタの情報を取得
 */
export declare class SmplPopupService {
    private apiComService;
    private popupCdSubject;
    popupCdState: import("rxjs").Observable<string>;
    constructor(apiComService: ShCtlApiService);
    private masterCache;
    /**
     * 汎用マスタ取得
     */
    fetchAll(url: string): Promise<any>;
    /**
     * 指定したマスタのキャッシュしているデータを取得
     * @param key マスタの名前
     */
    getMasterData(key: string): any[];
    /**
     * マスタデータをキャッシュする
     * @param key マスタの名前
     * @param masterData マスタデータ
     */
    setMasterData(key: string, masterData: any[]): void;
}
