import { ElementRef, AfterViewInit, OnChanges } from '@angular/core';
export declare class ShReadOnlyDirective implements AfterViewInit, OnChanges {
    private el;
    constructor(el: ElementRef);
    isReadOnly: boolean;
    ngAfterViewInit(): void;
    ngOnChanges(): void;
    addAttribute(): void;
}
