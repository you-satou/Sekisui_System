import { OnInit } from "@angular/core";
import { ShRedirectService } from 'sh-http-client';
/**
 * キー制御の初期化など
 */
export declare class ShAppComponent implements OnInit {
    private redirectService;
    /**
     * コンストラクタ
     */
    constructor(redirectService: ShRedirectService);
    /**
     * コンポーネント初期化
     */
    ngOnInit(): void;
    /**
     * 初期化
     */
    private init;
    private redirect;
    private setToekn;
    /**
     *@ エンター時にフォーカスを当てたくない場合の条件
     */
    private isTargetElement;
    private checkShCtlAttr;
    private getShCtlElement;
    /**
     * キーバリデーション付与
     */
    keyValidation(): void;
}
