import { OnInit, AfterViewChecked } from '@angular/core';
import { MatDialogRef } from '@angular/material';
interface Data {
    dataTitle: string;
    hiddenColumns: any;
}
export declare class SHCtlPopupDialogComponent implements OnInit, AfterViewChecked {
    private dialogRef;
    private data;
    displayedColumns: string[];
    name: string;
    uniqueStr: string;
    dataTitle: string;
    codes: any;
    unchangeableCodes: any;
    codesHeader: any;
    constructor(dialogRef: MatDialogRef<SHCtlPopupDialogComponent>, data: Data);
    /**
     * 値がバインドされる時に呼ばれる
     */
    ngOnInit(): void;
    /**
    * 値の初期化
    */
    private init;
    /**
    * テーブルのヘッダーを編集
    */
    ngAfterViewChecked(): void;
    /**
     * ダイアログを閉じる
     */
    closeDialog(): void;
    /**
     * ポップアップから選択されたデータを親に返す
     * @param item 事業所コード一覧から選択された1つ
     */
    selectCode(item: any): void;
    /**
     * データのkeyを返す
     */
    getHashKeys(): string[];
    /**
     * ポップアップ内での曖昧検索
     * スペース区切りで1キーワードとする
     * @param event inputイベント
     */
    filter(event: any): void;
}
export {};
