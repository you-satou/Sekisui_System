import { OnInit, OnChanges } from "@angular/core";
import { MatDialog } from "@angular/material";
import { SHFormPopup } from "../../utilities/ShFormPopup";
import { SmplPopupService } from "../../services/smpl-popup.service";
import { BindCtlService } from "../../services/bind-ctl.service";
import { LockScreenService } from "../../services/lock-screen.service";
export declare class SHCtlTanCodeComponent extends SHFormPopup implements OnInit, OnChanges {
    protected smplPopupService: SmplPopupService;
    protected dialog: MatDialog;
    protected bindCtlService: BindCtlService;
    protected lockScreenService: LockScreenService;
    obj: string;
    enabledHalfKanaInput: boolean;
    constructor(smplPopupService: SmplPopupService, dialog: MatDialog, bindCtlService: BindCtlService, lockScreenService: LockScreenService);
    /**
     * コンポーネントに値がバインドされた後で呼び出される
     * プロパティの初期化
     */
    ngOnInit(): void;
    /**
     * onInput
     * @param event input
     */
    onInput(event: any): void;
    /**
     * onChange
     * @param event change
     */
    onChange(event: any): void;
    /**
     * ダイアログを開いて閉じる時に値を親に返す
     */
    openDialog(): void;
    /**
     * 選択された区分コードに紐づく区分名を設定
     */
    setName(val: any): any;
    checkValue(value: any): boolean;
    /**
     * onClick
     * @param event click
     */
    onClick(event: any): void;
    /**
     * onFocus
     * @param event focus
     */
    onFocus(event: any): void;
    /**
     * onBlur
     * @param event blur
     */
    onBlur(event: any): void;
    /**
     * onKeyUp
     * @param event KeyUp
     */
    onKeyUp(event: any): void;
    /**
     * 先頭除去
     * @param inputTargetValue event.target.value
     */
    toText(inputTargetValue: any): any;
    /**
     * ひらがなをカタカナに変換
     * @param inputValue
     */
    hiraganaToKatagana(inputValue: any): any;
    /**
     * 入力文字を半角カナに変換
     * @param inputValue
     */
    toHankaku(inputValue: any): string;
}
