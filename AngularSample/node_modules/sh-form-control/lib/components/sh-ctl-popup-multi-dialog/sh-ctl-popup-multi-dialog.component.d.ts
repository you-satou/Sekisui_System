import { OnInit, AfterViewChecked } from '@angular/core';
import { MatDialogRef } from '@angular/material';
import { SelectionModel } from '@angular/cdk/collections';
interface Data {
    dataTitle: string;
    value: any;
    uniqueColumns: any;
    hiddenColumns: any;
}
export declare class SHCtlPopupDialogMultiComponent implements OnInit, AfterViewChecked {
    private dialogRef;
    data: Data;
    displayedColumns: string[];
    name: string;
    uniqueStr: string;
    dataTitle: string;
    codes: any;
    unchangeableCodes: any;
    codesHeader: any;
    selection: SelectionModel<any>;
    constructor(dialogRef: MatDialogRef<any>, data: Data);
    /**
     * 値がバインドされる時に呼ばれる
     */
    ngOnInit(): void;
    /**
    * 値の初期化
    */
    private init;
    /**
    * テーブルのヘッダーを編集
    */
    ngAfterViewChecked(): void;
    /**
     * チェックボックス選択
     */
    itemSelect(row: any): void;
    /**
     * 行選択によるチェックボックス値の操作
     */
    itemToggle(row: any): void;
    /**
     * 項目を選択しているかどうか
     */
    isItemSelected(row: any): boolean;
    /**
     * ダイアログを閉じる
     */
    closeDialog(): void;
    /**
     * ポップアップから選択されたデータを親に返す
     * @param item
     */
    returnDialog(): void;
    /**
     * 全ての項目を選択しているかどうか
     */
    isAllSelected(): boolean;
    /**
     * 全選択・全解除処理
     */
    masterToggle(): void;
    /**
     * チェックボックスラベル
     */
    checkboxLabel(row?: any): string;
    /**
     * データのkeyを返す
     */
    getHashKeys(): string[];
    /**
     * ポップアップ内での曖昧検索
     * スペース区切りで1キーワードとする
     * @param event inputイベント
     */
    filter(event: any): void;
}
export {};
