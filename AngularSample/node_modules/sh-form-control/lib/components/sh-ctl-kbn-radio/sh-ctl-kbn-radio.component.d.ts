import { OnInit, EventEmitter, OnChanges, SimpleChanges } from "@angular/core";
import { SmplKbnCodeType } from "../../entities/SmplKbnCodeType";
import { SmplKbnCodeService } from "../../services/smpl-kbn-code.service";
import { SHFormControl } from '../../utilities/ShFormControl';
export declare class SHCtlKbnRadioComponent extends SHFormControl<string> implements OnInit, OnChanges {
    private SmplKbnRadioService;
    dataKbnName: string;
    radioContent: any;
    apiUrl: string;
    displayLabel: string;
    throwError: EventEmitter<{}>;
    name: string;
    kbnCodes: SmplKbnCodeType[];
    constructor(SmplKbnRadioService: SmplKbnCodeService);
    /**
     *プロパティの初期化
     *通常サービスにキャッシュしているが、無ければ再度マスタより取得
     */
    ngOnInit(): void;
    /**
     * ngOnChanges
     */
    ngOnChanges(change: SimpleChanges): void;
    /**
     * onClick
     * @param event click
     */
    onClick(event: any): void;
    /**
     * onChange
     * @param event change
     */
    onChange(event: any): void;
    /**
     * onFocus
     * @param event focus
     */
    onFocus(event: any): void;
    /**
     * onBlur
     * @param event blur
     */
    onBlur(event: any): void;
    /**
     * フォーマット編集
     * @param value this.value
     */
    changeRadioContent(item: any): void;
}
