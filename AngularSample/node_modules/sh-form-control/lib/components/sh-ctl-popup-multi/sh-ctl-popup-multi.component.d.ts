import { OnInit } from "@angular/core";
import { SmplPopupService } from "../../services/smpl-popup.service";
import { MatDialog, MatDialogRef } from "@angular/material";
import { SHFormControl } from '../../utilities/ShFormControl';
import { SHCtlPopupDialogMultiComponent } from '../sh-ctl-popup-multi-dialog/sh-ctl-popup-multi-dialog.component';
export declare class ShCtlPopupMultiComponent extends SHFormControl<any> implements OnInit {
    protected SmplPopupService: SmplPopupService;
    protected dialog: MatDialog;
    dataTitle: string;
    apiUrl: string;
    dataCache: boolean;
    uniqueColumns: number[];
    hiddenColumns: number[];
    popupWidth: string;
    extendWidth: string;
    dialogRef: MatDialogRef<SHCtlPopupDialogMultiComponent, any>;
    codes: any[];
    codesHeader: any[];
    constructor(SmplPopupService: SmplPopupService, dialog: MatDialog);
    /**
     * ダイアログを開いて閉じる時に値を親に返す
     */
    openDialog(): void;
    /**
     *プロパティの初期化
     *通常サービスにキャッシュしているが、無ければ再度マスタより取得
     */
    ngOnInit(): void;
    /**
     * マスタ情報よりコードを取得
     */
    getCodes(): void;
}
