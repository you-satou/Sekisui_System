import { OnInit, EventEmitter, SimpleChanges } from "@angular/core";
import { SmplKbnCodeType } from "../../entities/SmplKbnCodeType";
import { SmplKbnCodeService } from "../../services/smpl-kbn-code.service";
import { SHFormControl } from '../../utilities/ShFormControl';
export declare class SHCtlKbnSelectComponent extends SHFormControl<string> implements OnInit {
    private SmplKbnSelectService;
    dataKbnName: string;
    selectContent: any;
    apiUrl: string;
    hiddenClearButton: boolean;
    throwError: EventEmitter<{}>;
    name: string;
    event: any;
    kbnCodes: SmplKbnCodeType[];
    uniqueStr: string;
    constructor(SmplKbnSelectService: SmplKbnCodeService);
    /**
     *プロパティの初期化
     *通常サービスにキャッシュしているが、無ければ再度マスタより取得
     */
    ngOnInit(): void;
    /**
     *itemsの変更検知
     *上位コンポーネントからitemsに値が注入されているか検知し変更する
     */
    ngOnChanges(change: SimpleChanges): void;
    /**
     * onClick
     * @param event click
     */
    onClick(event: any): void;
    /**
     * onChange
     * @param event change
     */
    onChange(): void;
    /**
     * onFocus
     * @param event focus
     */
    onFocus(event: any): void;
    /**
     * onBlur
     * @param event blur
     */
    onBlur(event: any): void;
    /**
     * フォーマット編集
     * @param value this.value
     */
    changeSelectContent(item: any): void;
    /**
     * 値のクリア
     */
    clearValue(): void;
}
